import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'book-in-percent',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
